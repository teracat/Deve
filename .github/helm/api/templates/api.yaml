apiVersion: v1
kind: Secret
metadata:
  name: api
  namespace: {{ .Values.namespace.name | quote }}
type: Opaque
stringData:
  yourModuleConnectionString: {{ .Values.api.yourModuleConnectionString | quote }}
  jwtSigningSecretKey: {{ required "api.jwtSigningSecretKey is required" .Values.api.jwtSigningSecretKey | quote }}
  jwtEncryptionSecretKey: {{ required "api.jwtEncryptionSecretKey is required" .Values.api.jwtEncryptionSecretKey | quote }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-cert
  namespace: {{ .Values.namespace.name | quote }}
spec:
  secretName: api-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: {{ .Values.api.host | quote }}
  dnsNames:
    - {{ .Values.api.host | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: {{ .Values.namespace.name | quote }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: "{{ .Values.acrLoginServer }}/{{ .Values.image.repositories.api }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.api.port }}
          securityContext:
            allowPrivilegeEscalation: false
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:{{ .Values.api.port }}"
            - name: ConnectionStrings__RedisCacheConnection
              value: "redis:{{ .Values.redis.port }}"
            - name: PROMETHEUS_SCRAPE_ENDPOINT
              value: "/metrics"
            - name: ZIPKIN_URL
              value: "http://zipkin.monitoring.svc.cluster.local:9411"
            - name: OTEL_SERVICE_NAME
              value: "api"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.instance.id=$(uuidgen)"
            - name: ConnectionStrings__YourModuleConnection
              valueFrom:
                secretKeyRef:
                  name: api
                  key: yourModuleConnectionString
            - name: AppSettings__JwtKeys__SigningSecretKey
              valueFrom:
                secretKeyRef:
                  name: api
                  key: jwtSigningSecretKey
            - name: AppSettings__JwtKeys__EncryptionSecretKey
              valueFrom:
                secretKeyRef:
                  name: api
                  key: jwtEncryptionSecretKey
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: {{ .Values.namespace.name | quote }}
spec:
  selector:
    app: api
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
  namespace: {{ .Values.namespace.name | quote }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
    acme.cert-manager.io/http01-ingress-class: nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.api.host | quote }}
      secretName: api-tls
  rules:
    - host: {{ .Values.api.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: {{ .Values.api.port }}
