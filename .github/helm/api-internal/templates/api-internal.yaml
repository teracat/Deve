apiVersion: v1
kind: Secret
metadata:
  name: api-internal
  namespace: {{ .Values.namespace.name | quote }}
type: Opaque
stringData:
  dataSourceConnectionString: {{ .Values.apiInternal.dataSourceConnectionString | quote }}
  jwtSigningSecretKey: {{ required "apiInternal.jwtSigningSecretKey is required" .Values.apiInternal.jwtSigningSecretKey | quote }}
  jwtEncryptionSecretKey: {{ required "apiInternal.jwtEncryptionSecretKey is required" .Values.apiInternal.jwtEncryptionSecretKey | quote }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-internal-cert
  namespace: {{ .Values.namespace.name | quote }}
spec:
  secretName: api-internal-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: {{ .Values.apiInternal.host | quote }}
  dnsNames:
    - {{ .Values.apiInternal.host | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-internal
  namespace: {{ .Values.namespace.name | quote }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-internal
  template:
    metadata:
      labels:
        app: api-internal
    spec:
      containers:
        - name: api-internal
          image: "{{ .Values.acrLoginServer }}/{{ .Values.image.repositories.apiInternal }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.apiInternal.port }}
          securityContext:
            allowPrivilegeEscalation: false
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:{{ .Values.apiInternal.port }}"
            - name: ConnectionStrings__RedisCacheConnection
              value: "redis-internal:{{ .Values.redis.port }}"
            - name: PROMETHEUS_SCRAPE_ENDPOINT
              value: "/metrics"
            - name: ZIPKIN_URL
              value: "http://zipkin.monitoring.svc.cluster.local:9411"
            - name: OTEL_SERVICE_NAME
              value: "api-internal"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.instance.id=$(uuidgen)"
            - name: ConnectionStrings__DataSourceConnection
              valueFrom:
                secretKeyRef:
                  name: api-internal
                  key: dataSourceConnectionString
            - name: AppSettings__JwtKeys__SigningSecretKey
              valueFrom:
                secretKeyRef:
                  name: api-internal
                  key: jwtSigningSecretKey
            - name: AppSettings__JwtKeys__EncryptionSecretKey
              valueFrom:
                secretKeyRef:
                  name: api-internal
                  key: jwtEncryptionSecretKey
---
apiVersion: v1
kind: Service
metadata:
  name: api-internal
  namespace: {{ .Values.namespace.name | quote }}
spec:
  selector:
    app: api-internal
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.apiInternal.port }}
      targetPort: {{ .Values.apiInternal.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-internal-ingress
  namespace: {{ .Values.namespace.name | quote }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
    acme.cert-manager.io/http01-ingress-class: nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - {{ .Values.apiInternal.host | quote }}
      secretName: api-internal-tls
  rules:
    - host: {{ .Values.apiInternal.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-internal
                port:
                  number: {{ .Values.apiInternal.port }}
