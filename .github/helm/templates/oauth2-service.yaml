apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
spec:
  ports:
    - port: {{ .Values.services.oauth2.externalPort }}
      targetPort: {{ .Values.services.oauth2.internalPort }}
  selector:
    app: oauth2-proxy