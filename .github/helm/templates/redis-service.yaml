{{- if has "redis" .Values.deploy.enabledServices }}
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: redis
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.services.redis.port }}
      targetPort: {{ .Values.services.redis.port }}
{{- end }}
