{{- if has "api-external" .Values.deploy.enabledServices }}
apiVersion: v1
kind: Service
metadata:
  name: api-external
spec:
  selector:
    app: api-external
  type: LoadBalancer
  ports:
    - name: http
      nodePort: {{ .Values.services.api-external.externalPort }}
      port: {{ .Values.services.api-external.externalPort }}
      targetPort: {{ .Values.services.api-external.internalPort }}
{{- end }}
