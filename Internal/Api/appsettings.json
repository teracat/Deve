{
  "AppSettings": {
    "JwtKeys": {
      "SigningSecretKey": "YouShouldChangeThisKey_MustBe30B", // You should change this key (must be 32 bytes)
      "EncryptionSecretKey": "YouShouldChangeThisKey_MustBe31B" // You should change this key (must be 32 bytes)
    },
    "SentryDsn": "" // You should set the SentryDsn (if using Sentry)"
  },
  "ConnectionStrings": {
    //You should set the DataSourceConnection (if needed by your DataSource)
    "DataSourceConnection": "Server=localhost;Database=dbname;User Id=user;Password=pwd;",
    //You should set the RedisCacheConnection (if using RedisCache class as the ICache implementation)
    "RedisCacheConnection": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
