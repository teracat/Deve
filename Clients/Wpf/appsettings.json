{
  // Connection strings
  "ConnectionStrings": {
    // <hooks:wpf-appsettings-connectionstrings>
    //You should set the CustomersConnection (if needed by your module)
    "CustomersConnection": "",
    //You should set the IdentityConnection (if needed by your module)
    "IdentityConnection": ""
  }
}
